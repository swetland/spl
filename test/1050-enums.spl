
var i i32 = 3;

enum { ZERO, ONE, TWO, };

enum { A = 5, B = 6, C = 0x10000000, D = 0x10000001, E = 50 + 50, F = ONE * TWO, };

fn start() i32 {
	_hexout_(ZERO);
	_hexout_(ONE);
	_hexout_(TWO);
	_hexout_(A);
	_hexout_(B);
	_hexout_(C);
	_hexout_(D);
	_hexout_(E);
	_hexout_(F);
        return 0;
}

